{
  "name": "moodreader-mvp",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "extension",
    "server"
  ],
  "scripts": {
    "dev:ext": "npm run dev -w extension",
    "build:ext": "npm run build -w extension",
    "dev:server": "npm run dev -w server",
    "build:server": "npm run build -w server",
    "test": "npm test --workspaces"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "prettier": "^3.2.4",
    "typescript": "^5.3.3"
  }
}
